/**
 * Shared geometry types for the camera-roll zoom transition.
 */

/** Axis-aligned rectangle in window coordinates. */
export interface Frame {
  x: number;
  y: number;
  width: number;
  height: number;
}
